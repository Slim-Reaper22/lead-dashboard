<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard Settings</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    .settings-header {
      background: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
    }
  </style>
</head>
<body class="bg-gray-100">
  <div class="settings-header text-white shadow-lg">
    <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8 flex justify-between items-center">
      <div>
        <h1 class="text-3xl font-bold">Dashboard Settings</h1>
        <p class="text-indigo-100">Configure your lead dashboard</p>
      </div>
      <div class="flex space-x-4">
        <a href="/" class="px-4 py-2 rounded-md bg-indigo-800 hover:bg-indigo-900 transition duration-150">
          <i class="fas fa-arrow-left mr-2"></i> Back to Dashboard
        </a>
        <a href="/logout" class="px-4 py-2 rounded-md bg-red-600 hover:bg-red-700 transition duration-150">
          <i class="fas fa-sign-out-alt mr-2"></i> Logout
        </a>
      </div>
    </div>
  </div>

  <div class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
    <% if (message) { %>
      <div class="mb-6 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline"><%= message %></span>
      </div>
    <% } %>
    
    <div class="bg-white rounded-lg shadow overflow-hidden mb-6">
      <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
        <h3 class="text-lg font-medium text-gray-900">SmartSuite API Configuration</h3>
      </div>
      <div class="p-6">
        <form action="/update-api-config" method="POST">
          <div class="grid grid-cols-1 gap-6 sm:grid-cols-2">
            <div class="col-span-2">
              <label for="apiKey" class="block text-sm font-medium text-gray-700">API Key</label>
              <input type="password" name="apiKey" id="apiKey" 
                value="<%= config.apiKey %>" 
                class="mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md"
                placeholder="Enter your SmartSuite API key">
              <p class="mt-1 text-sm text-gray-500">Your SmartSuite API key will be stored securely.</p>
            </div>
            
            <div class="col-span-2">
              <label for="accountId" class="block text-sm font-medium text-gray-700">Account/Workspace ID</label>
              <input type="text" name="accountId" id="accountId" 
                value="<%= config.accountId %>" 
                class="mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md"
                placeholder="SmartSuite Account/Workspace ID">
              <p class="mt-1 text-sm text-gray-500">Required - The ACCOUNT-ID header value for API calls.</p>
            </div>
            
            <div>
              <label for="appId" class="block text-sm font-medium text-gray-700">Application ID</label>
              <input type="text" name="appId" id="appId" 
                value="<%= config.appId %>" 
                class="mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md"
                placeholder="SmartSuite Application ID">
            </div>
            
            <div>
              <label for="tableId" class="block text-sm font-medium text-gray-700">Table ID</label>
              <input type="text" name="tableId" id="tableId" 
                value="<%= config.tableId %>" 
                class="mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md"
                placeholder="SmartSuite Table ID">
            </div>
          </div>
          
          <div class="mt-6">
            <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
              <i class="fas fa-save mr-2"></i> Save Configuration
            </button>
          </div>
        </form>
      </div>
    </div>
    
    <div class="bg-white rounded-lg shadow overflow-hidden">
      <div class="px-6 py-4 border-b border-gray-200 bg-gray-50">
        <h3 class="text-lg font-medium text-gray-900">Data Management</h3>
      </div>
      <div class="p-6">
        <form action="/refresh-data" method="POST">
          <p class="mb-4 text-sm text-gray-600">Manually refresh data from the SmartSuite API.</p>
          <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
            <i class="fas fa-sync-alt mr-2"></i> Refresh Data Now
          </button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>
